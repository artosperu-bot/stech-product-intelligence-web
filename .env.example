# Cloud Run / runtime
STECH_HEADLESS=true
ARTIFACT_TTL_MINUTES=30
# Optional: authenticated Playwright storage state JSON for chatgpt.com.
# Prefer Google Secret Manager instead of plain env vars in production.
CHATGPT_STORAGE_STATE_JSON=
